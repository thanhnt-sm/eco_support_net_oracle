---
phase: 8
title: "Claim contract and coverage"
status: in-progress
priority: P1
effort: "M"
dependencies: [1]
---

# Phase 8: Claim contract and coverage

## Overview

Owner selected full feature delivery. Run immediately after phase1/CP0–CP1, BEFORE source changes. Scope amendment overrides old docs-only closure. Terra sole writer; Sol read-only.

## Requirements

Inventory every current-doc capability occurrence, not only the health/CVE/settings examples. Preserve66 parents and15RT children; expand FC01–FC20 groups into individual testable claims. No remove/downgrade-to-close. Historical evidence/proposals stay classified, not revived as current features.

## Architecture

**XR09 — distribution contract:** CP8 freezes an artifact matrix before project creation: Host standalone executable archive and supported RIDs/runtime prerequisites/launch command (CLI serve remains optional, usable Host delivery does not); LanguageServer embedded per supported VSCode VSIX platform with extension-relative immutable path, checksums, framework/runtime dependency closure and coordinated update policy; DataGuard.Build nupkg target/task TFM plus explicit `build`/`buildTransitive` import policy and runtime dependencies. No download-on-editor-start. Assign Terra file leases for `.github/workflows/build_release.yml` packaging changes in the matching feature phase, then final integration in Phase15; never dispatch or publish without authority. Record supported platform exclusions as open/blocked claims, not inferred support. Package contents and clean install-from-artifact commands are required ACs.

A claim occurrence maps doc path/line/text + EN/VI peer → existing source/caller → target phase/API → test + platform/mode + evidence + state. FC groups are containers, not a false claim there are only20 product claims. Source of truth: full-claims-ledger.md; no parent closes before required children.

## Related Code Files

- Plan-only outputs: full-claims-ledger.md; proposed reports/full-claims-evidence.md and reports/claim-occurrences.md.
- Read baseline143 docs (classify7 raw logs), changed/new docs, root README/README.vi/SECURITY and extension READMEs. Reuse original scout inventory.
- Read Core models/public contracts, CLI Program, both IDE manifests, analyzer/codefix APIs and DataGuard.sln.

## Implementation Steps

1. Preserve WIP/execution-evidence and refresh fingerprint. Tell Sol prior docs-only closure is superseded.
2. Read/classify every in-scope document; produce per-file ledger and individual claim rows, including duplicate wording aliases. Sol checks unmatched occurrences.
3. Freeze APIs/options/namespaces for Host, Build, LanguageServer, advisory/score, integrity and exports. Keep existing public constructors/method returns ABI-safe.
4. Freeze operator consent: no network/DB per editor keystroke or default build. Workspace YAML cannot grant trusted executable/artifact/remote egress permission.
5. Resolve five-versus12 codefix taxonomy: retain five named providers;12-provider claim requires12 distinct tested roles, not empty classes. Freeze exact provider/ID/action map before implementation. Unresolved contradiction remains owner-gated.
6. Freeze compatibility facade namespaces for DataContract/SqlParameter/ResultSet/DataGuard.Validate with collision/semantics tests; do not silently map away promised APIs.
7. Define bounded EF generated-C# and static procedure-body support matrices, explicit unsupported status, no dynamic execution/no hidden build fallback.
8. Freeze benchmark hardware/scenario/thresholds before optimization. Absolute no-memory-dump/zero-allocation/zero-vendor-dependency claims need explicit threat/architecture boundary or owner decision, never fabricated proof.
9. Verify current runtime support and net9 Core/net472 VS interoperability before choosing Host/Build/LSP targets; no blanket solution migration assumed.
10. Sol CP8 GO requires claim census, individual acceptance, leases, runtime/ABI/privacy and unresolved decisions.

## Success Criteria

- [x] Every current-doc file classified and occurrence mapped; the generated census reports 539 mapped candidate occurrences and 0 unmatched command candidates. FC20 remains open for prose classification beyond the conservative scanner.
- [x] Required feature groups expanded into subrows, delivery phases and evidence tiers.
- [x] Scope amendment replaces docs-only throughout active handoff.
- [x] Contradictory/absolute claims remain explicit open owner gates until resolved.
- [ ] Sol CP8 GO before implementation.

## Gate status

**Sol CP8 verdict: NO-GO (2026-09-18).** The 539-entry candidate scan is deliberately
conservative; FC20 still lacks exhaustive prose classification, and individual occurrences lack
the required source/caller, test/platform/mode and final-evidence mappings. Complete that census
and the CP1 gate, then obtain a new CP8 review before any remediation source work proceeds.

## Risk Assessment

More modules justified by distinct HTTP, editor, build and remote trust boundaries. Reuse Core rather than duplicate engine. Execution follows DAG, not phase numbering. A foundation_verified milestone is not full feature closure.
